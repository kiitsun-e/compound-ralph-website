---
title: cr init
description: Initialize a project for Compound Ralph with auto-detected configuration
---

Initialize a project for Compound Ralph by creating the necessary directory structure and configuration files.

## Usage

```bash
cr init [path]
```

## Arguments

| Argument | Description | Default |
|----------|-------------|---------|
| `path` | Directory to initialize | Current directory |

## What It Creates

Running `cr init` sets up the following:

```
your-project/
├── specs/           # Feature specifications go here
├── plans/           # Rich plans from cr plan
├── .cr/             # Project configuration
│   └── project.json # Auto-detected commands
└── AGENTS.md        # Build/test/lint commands
```

### Auto-Detection

Compound Ralph detects your project type and configures appropriate commands:

| Project Type | Detection File | Build Command | Test Command |
|-------------|----------------|---------------|--------------|
| Bun | `bun.lockb` | `bun run build` | `bun test` |
| npm | `package-lock.json` | `npm run build` | `npm test` |
| Rails | `Gemfile` | `bin/rails assets:precompile` | `bin/rails test` |
| Python | `pyproject.toml` | `pip install -e .` | `pytest` |
| Go | `go.mod` | `go build` | `go test ./...` |

## Examples

### Initialize current directory

```bash
cd my-project
cr init
```

Output:
```
✓ Created specs/
✓ Created plans/
✓ Created .cr/project.json
✓ Detected: bun (bun.lockb found)
✓ Generated AGENTS.md

Project initialized for Compound Ralph.
Next: cr plan "your feature description"
```

### Initialize a specific path

```bash
cr init ~/projects/my-app
```

### Re-initialize to update detection

If you add a lockfile or change project structure:

```bash
cr init
# Re-detects project type and updates configuration
```

## Related Commands

- [cr plan](/commands/plan) - Create a feature plan
- [cr status](/commands/status) - Check project status
